[phases.setup]
nixPkgs = ["python312", "libsndfile"]

[phases.install]
cmds = [
    "pip install uv",
    "uv sync --frozen --extra-index-url https://download.pytorch.org/whl/cpu"
]

[phases.build]
cmds = []

[start]
cmd = "python -m api"