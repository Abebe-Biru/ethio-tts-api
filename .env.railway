# Railway Environment Variables Template
# Copy these to Railway's environment variables section

# REQUIRED: Generate a strong secret (32+ characters)
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
WEBHOOK_SECRET=CHANGE_THIS_TO_A_STRONG_SECRET_KEY

# Server Configuration (Railway sets PORT automatically)
HOST=0.0.0.0
DEBUG=false

# CORS Configuration
# Change to your frontend domain in production
CORS_ORIGINS=*

# Cache Configuration
CACHE_DIR=tts_cache

# Optional: If you add PostgreSQL later
# DATABASE_URL=${{Postgres.DATABASE_URL}}
